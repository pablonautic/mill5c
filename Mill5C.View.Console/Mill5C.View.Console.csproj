<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net6.0</TargetFramework>
    <RootNamespace>Mill5C.View.Console</RootNamespace>
    <AssemblyName>MillingSimulator.Console</AssemblyName>
    <FileAlignment>512</FileAlignment>
    <!-- System.Windows.Forms reference was present but likely not needed for a console app.
         If it is truly used, this project might need <UseWindowsForms>true</UseWindowsForms>
         and TargetFramework net6.0-windows. Assuming it's not essential for console. -->
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\Mill5C.Core\Mill5C.Core.csproj" />
    <ProjectReference Include="..\Mill5C.Settings\Mill5C.Settings.csproj" />
  </ItemGroup>

</Project>