<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net40</TargetFramework> <!-- Objective 3 -->
    <GenerateAssemblyInfo>false</GenerateAssemblyInfo> <!-- Objective 4 -->
    <Configurations>Debug;Release</Configurations>
    <Platforms>AnyCPU;x86</Platforms>
    <!-- AssemblyName and RootNamespace will be inferred from directory/file name or can be added if different -->
  </PropertyGroup>

  <!-- Conditional properties from original project (Objective 8) -->
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <OutputPath>bin\Debug\</OutputPath>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>pdbonly</DebugType>
    <Optimize>true</Optimize>
    <OutputPath>bin\Release\</OutputPath>
    <DefineConstants>TRACE</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|x86' ">
    <PlatformTarget>x86</PlatformTarget>
    <OutputPath>bin\x86\Debug\</OutputPath>
    <!-- Assuming Debug symbols & constants for Debug|x86 based on Debug|AnyCPU -->
    <DebugSymbols>true</DebugSymbols>
    <DebugType>full</DebugType>
    <Optimize>false</Optimize>
    <DefineConstants>DEBUG;TRACE</DefineConstants>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|x86' ">
    <PlatformTarget>x86</PlatformTarget>
    <OutputPath>bin\x86\Release\</OutputPath>
    <!-- Assuming Release optimize & constants for Release|x86 based on Release|AnyCPU -->
    <Optimize>true</Optimize>
    <DebugType>pdbonly</DebugType>
    <DefineConstants>TRACE</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <!-- Test Framework Reference (Objective 6) -->
    <Reference Include="Microsoft.VisualStudio.QualityTools.UnitTestFramework, Version=9.0.0.0, Culture=neutral, PublicKeyToken=b03f5f7f11d50a3a, processorArchitecture=MSIL" />
    <!-- System and System.Core are implicitly referenced by net40 SDK -->
  </ItemGroup>

  <ItemGroup>
    <!-- Project References (Objective 5) -->
    <ProjectReference Include="..\Mill5C.Core\Mill5C.Core.csproj" />
    <ProjectReference Include="..\Mill5C.Settings\Mill5C.Settings.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Explicitly include AssemblyInfo.cs because GenerateAssemblyInfo is false (Objective 4) -->
    <Compile Include="Properties\AssemblyInfo.cs" />
  </ItemGroup>

</Project>
